#include <bits/stdc++.h>
using namespace std;


/* binary tree
void dfs(int node) {
    base case
    if does not  have anychild
        return

    general case
    dfs(node.left_child)
    dfs(node.right_child)
}
*/


/* n-ary tree
void dfs(int node) {
    base case
    if does not  have anychild
        return

    for all childs of node:
        dfs(child)
}
*/


/* 

    *graph
    * visited array
    
    cout << node << endl;
    visited[node] = 1;

    for all adj_node of node:
        if visited[adj_node] = 0:
            dfs(adj_node)


*/


int main() {

}